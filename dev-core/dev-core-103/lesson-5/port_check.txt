При вводе команды 'nc localhost 8080'
Был вывод 'Hello, World!'
Означает что порт занят. 

Порт не входит в первые 1023, а значит он скорее всего свободен и не требует root-доступпа

При смене порта на 8000 значимых изменнеий не было

Также можно проверить благодаря команде 'lsof -i :port' (Вместо port ввести числовое значение порта)